/* 3단원 파이썬 확장 — 지출 합계 · 요금제 기간별 비교
   기본 문제: 리스트 · for · if, 빈칸(✏️) 1줄 / 도전 문제: CSV · 분류 함수 · 딕셔너리 누적 */
module.exports = {
  unit: 3,
  missions: [
    { part: 4, n: 'P6', h: '지출 합계를 파이썬으로 검산하고, 요금제를 기간별로 비교하기', body: `
      <p>미션 9·10의 <strong>가상 지출 메모</strong>와 <strong>가상 요금제</strong>를 파이썬으로 계산해서, AI가 말한 숫자와 대조합니다. 그리고 요금제는 <strong>몇 개월을 쓰느냐에 따라 가장 싼 것이 바뀐다</strong>는 것을 확인합니다.</p>
      <p><strong>쓰는 파이썬:</strong> 리스트 · <code>sum()</code> · <code>for</code> + <code>if</code> · 곱셈 · 글자 막대그래프</p>
      __NB__
      <ol>
        <li>노트북 <strong>1-1</strong>: 지출 항목·금액·분류 리스트를 실행하고 전체 합계를 확인합니다. (정답 252,600원)</li>
        <li><strong>1-2 ✏️</strong>: 빈칸 한 줄 — 분류가 "식비"인 금액만 더합니다. (정답 88,300원)</li>
        <li><strong>1-3</strong>: 분류별 합계와 막대그래프를 보고, 실습지에 적은 <strong>AI 합계</strong>를 넣어 대조합니다.</li>
        <li><strong>2-1 ✏️</strong>: 빈칸 한 줄 — "나 요금제" 12개월 총요금 계산식을 완성합니다. (정답 366,000원)</li>
        <li><strong>2-2</strong>: 1~24개월 비교표에서 <strong>가장 싼 요금제가 바뀌는 달</strong>을 찾아 실습지에 적습니다.</li>
      </ol>
      <div class="box check"><span class="lbl">발견할 것</span>
        <p>1년이면 "나"가 가장 싸지만, <strong>18개월째부터는 "가"가 더 쌉니다.</strong> AI가 "나가 제일 싸요"라고만 했다면 <strong>몇 개월 기준</strong>인지 빠뜨린 것입니다.</p></div>
      <div class="box warn"><span class="lbl">주의</span>
        <p>내 지출로 해 볼 때도 <strong>항목·금액</strong>만 씁니다. 카드·계좌번호나 명세서 원본은 넣지 않습니다.</p></div>
      <div class="box check"><span class="lbl">도전 문제 (선택)</span>
        <p>노트북 끝 <strong>도전</strong>: 지출 메모를 CSV 파일로 저장·읽기 → 항목 이름으로 <strong>자동 분류</strong>하는 함수 → 딕셔너리로 분류별 합계 → matplotlib 그래프.</p></div>` }
  ],

  notebook: {
    file: 'unit3_python.ipynb',
    title: '3단원 파이썬 확장 — 지출 합계 검산 · 요금제 기간별 비교',
    cells: [
      { md: `# 3단원 파이썬 확장 실습
**디지털리터러시 36시간 과정 · 실생활에 유용한 AI 활용 · 4차시 가계·소비**

| 번호 | 내용 | 빈칸 |
|---|---|---|
| 1 | 지출 합계 · 식비 합계 · 분류별 막대그래프 · AI 답 대조 | 1줄 |
| 2 | 요금제 12개월 총요금 · 1~24개월 비교표 | 1줄 |
| 도전 | CSV 파일 · 자동 분류 함수 · 딕셔너리 합계 · 그래프 | 여러 줄 |

- **Shift + Enter**로 위에서부터 실행합니다. **✏️ 줄**만 고치면 됩니다.
- 모든 데이터는 **수업용 가상 데이터**입니다. 실제 카드·계좌 정보는 넣지 않습니다.` },
      { code: String.raw`
# 정답 확인 도우미 — 맨 먼저 한 번 실행하세요
def 확인(이름, 결과, 정답):
    if 결과 is None or 결과 == 0:
        print("⏳", 이름, ": 아직 비어 있어요")
    elif 결과 == 정답:
        print("✅", 이름, ": 정답입니다!", format(결과, ","), "원")
    else:
        print("❌", 이름, ": 결과", format(결과, ","), "/ 기대", format(정답, ","))

print("준비 완료")` },

      { md: `---
## 1. 지출 합계 검산

### 1-1. 지출 목록
같은 번호끼리 한 줄입니다. 예) \`항목[0]\` = "동네 마트 장보기", \`금액[0]\` = 54300, \`분류[0]\` = "식비"
분류는 미션 9에서 AI가 나눈 결과를 옮겨 적은 것입니다.` },
      { code: String.raw`
항목 = ["동네 마트 장보기", "교통카드 충전", "휴대폰 요금", "약국 감기약", "세제·휴지",
        "친구와 점심", "동네 병원 진료비", "인터넷 요금", "시장 반찬", "손주 생일 선물"]
금액 = [54300, 30000, 48000, 6500, 17800, 22000, 5000, 22000, 12000, 35000]
분류 = ["식비", "교통", "통신", "의료", "생활용품", "식비", "의료", "통신", "식비", "기타"]

for i in range(len(항목)):
    print(항목[i], ":", format(금액[i], ","), "원 /", 분류[i])

전체_합계 = sum(금액)
print()
print("전체 합계:", format(전체_합계, ","), "원")` },

      { md: `### 1-2. ✏️ 식비만 더하기
분류가 \`"식비"\`인 줄의 금액만 \`식비_합계\`에 더합니다.` },
      { code: String.raw`
식비_합계 = 0
for i in range(len(금액)):
    if 분류[i] == "식비":
        pass   # ✏️ 빈칸: pass를 지우고 식비_합계에 금액[i]를 더하세요

print("식비 합계:", format(식비_합계, ","), "원")
확인("식비 합계", 식비_합계, 88300)`,
        answer: String.raw`
식비_합계 = 0
for i in range(len(금액)):
    if 분류[i] == "식비":
        식비_합계 = 식비_합계 + 금액[i]

print("식비 합계:", format(식비_합계, ","), "원")
확인("식비 합계", 식비_합계, 88300)` },

      { md: `### 1-3. 분류별 합계 · 막대그래프 · AI 답 대조
1-2와 **같은 방법**을 분류마다 반복합니다. ■ 하나는 1만 원입니다.` },
      { code: String.raw`
분류목록 = ["식비", "교통", "통신", "의료", "생활용품", "기타"]

for 이름 in 분류목록:
    합 = 0
    for i in range(len(금액)):
        if 분류[i] == 이름:
            합 = 합 + 금액[i]
    비율 = 합 / 전체_합계 * 100
    print(이름, ":", format(합, ","), "원", round(비율, 1), "%", "■" * (합 // 10000))` },
      { code: String.raw`
# ✏️ 실습지 미션 9에서 AI가 말한 전체 합계를 적으세요 (쉼표 없이)
AI_합계 = 252600
계산기_합계 = 252600    # 내가 휴대폰 계산기로 구한 값

print("AI     :", format(AI_합계, ","), "원")
print("계산기 :", format(계산기_합계, ","), "원")
print("파이썬 :", format(전체_합계, ","), "원")
if AI_합계 == 계산기_합계 == 전체_합계:
    print("✅ 세 값이 모두 같습니다")
else:
    print("⚠️ 다른 값이 있습니다. 어느 쪽이 틀렸는지 항목별로 찾아보세요")` },

      { md: `---
## 2. 요금제, 기간을 바꾸면 답이 바뀐다

미션 10의 **가상 요금제**입니다. (실제 통신사 상품이 아닙니다)

| 요금제 | 조건 |
|---|---|
| 가 | 매달 33,000원 |
| 나 | 처음 6개월 22,000원, 7개월째부터 39,000원 |
| 다 | 매달 45,000원 |

### 2-1. ✏️ 12개월 총요금
"나"는 **처음 6개월 × 22,000원 + 나머지 6개월 × 39,000원**입니다.` },
      { code: String.raw`
개월 = 12

가_총요금 = 33000 * 개월
나_총요금 = None   # ✏️ 빈칸: 22000 * 6 + 39000 * (개월 - 6)
다_총요금 = 45000 * 개월

print("가:", 가_총요금, "원")
print("나:", 나_총요금, "원")
print("다:", 다_총요금, "원")
확인("가 12개월", 가_총요금, 396000)
확인("나 12개월", 나_총요금, 366000)
확인("다 12개월", 다_총요금, 540000)`,
        answer: String.raw`
개월 = 12

가_총요금 = 33000 * 개월
나_총요금 = 22000 * 6 + 39000 * (개월 - 6)
다_총요금 = 45000 * 개월

print("가:", 가_총요금, "원")
print("나:", 나_총요금, "원")
print("다:", 다_총요금, "원")
확인("가 12개월", 가_총요금, 396000)
확인("나 12개월", 나_총요금, 366000)
확인("다 12개월", 다_총요금, 540000)` },

      { md: `### 2-2. 1개월부터 24개월까지 비교표
2-1의 계산을 **1~24개월에 대해 반복**합니다. 가장 싼 요금제가 바뀌는 달에 표시가 붙습니다.` },
      { code: String.raw`
print("개월 |      가 |      나 |      다 | 가장 쌈")
이전 = ""
for 개월 in range(1, 25):
    가 = 33000 * 개월
    if 개월 <= 6:
        나 = 22000 * 개월
    else:
        나 = 22000 * 6 + 39000 * (개월 - 6)
    다 = 45000 * 개월

    if 가 < 나:
        가장쌈 = "가"
    elif 나 < 가:
        가장쌈 = "나"
    else:
        가장쌈 = "가=나 (같음)"

    표시 = ""
    if 이전 != "" and 가장쌈 != 이전 and "같음" not in 가장쌈:
        표시 = " ← 바뀜!"
    if "같음" not in 가장쌈:
        이전 = 가장쌈
    print(f"{개월:>4} | {가:>7,} | {나:>7,} | {다:>7,} | {가장쌈}{표시}")` },
      { md: `#### 💬 생각해 보기 (실습지에 적기)
- 12개월 기준으로 가장 싼 요금제는? 24개월 기준으로는?
- 두 요금제가 **같아지는 달**과 **순위가 바뀌는 달**은?
- ("다"는 매달 가장 비싸서 표에서 뽑히지 않습니다)

🤖 **AI에게 다시 묻기**: "위 요금제 3개를 1년 기준과 2년 기준으로 각각 비교하고, 몇 개월째부터 순위가 바뀌는지 알려 줘"
→ AI 답과 위 표를 대조하세요. 👉 **조건(기간)을 분명히 줄수록 답이 정확해지고, 계산은 직접 검증합니다.**` },

      { md: `---
# 🏆 도전 문제 (선택)
**파일 입출력 · csv 모듈 · 함수 · 딕셔너리 누적**을 씁니다.

## 도전 A. 지출 메모를 CSV로 저장하고 읽기` },
      { code: String.raw`
import csv

지출_메모 = """날짜,항목,금액
3/2,동네 마트 장보기,54300
3/3,교통카드 충전,30000
3/5,휴대폰 요금,48000
3/8,약국 감기약,6500
3/10,세제·휴지,17800
3/12,친구와 점심,22000
3/15,동네 병원 진료비,5000
3/18,인터넷 요금,22000
3/21,시장 반찬,12000
3/25,손주 생일 선물,35000
"""
with open("지출.csv", "w", encoding="utf-8") as f:
    f.write(지출_메모)

지출들 = []
with open("지출.csv", encoding="utf-8") as f:
    for 줄 in csv.DictReader(f):
        줄["금액"] = int(줄["금액"])
        지출들.append(줄)
print(len(지출들), "건 읽음:", 지출들[0])` },
      { md: `## 도전 B. ✏️ 항목 이름으로 자동 분류하는 함수
항목 이름에 규칙의 낱말이 **들어 있으면** 그 분류, 없으면 \`"기타"\`. 힌트: \`for 분류, 낱말들 in 분류_규칙.items():\` 안에서 \`if 낱말 in 항목:\`` },
      { code: String.raw`
분류_규칙 = {
    "식비": ["마트", "점심", "반찬"],
    "교통": ["교통카드"],
    "통신": ["휴대폰", "인터넷"],
    "의료": ["약국", "병원"],
    "생활용품": ["세제", "휴지"],
}

def 분류하기(항목):
    # ✏️ TODO: 규칙을 돌면서 낱말이 항목에 들어 있으면 그 분류를 return
    return "기타"

for 분류_이름, 기대 in [("약국 감기약", "의료"), ("인터넷 요금", "통신"), ("손주 생일 선물", "기타")]:
    print("✅" if 분류하기(분류_이름) == 기대 else "❌", 분류_이름, "→", 분류하기(분류_이름))`,
        answer: String.raw`
분류_규칙 = {
    "식비": ["마트", "점심", "반찬"],
    "교통": ["교통카드"],
    "통신": ["휴대폰", "인터넷"],
    "의료": ["약국", "병원"],
    "생활용품": ["세제", "휴지"],
}

def 분류하기(항목):
    for 분류_이름, 낱말들 in 분류_규칙.items():
        for 낱말 in 낱말들:
            if 낱말 in 항목:
                return 분류_이름
    return "기타"

for 분류_이름, 기대 in [("약국 감기약", "의료"), ("인터넷 요금", "통신"), ("손주 생일 선물", "기타")]:
    print("✅" if 분류하기(분류_이름) == 기대 else "❌", 분류_이름, "→", 분류하기(분류_이름))` },
      { md: `## 도전 C. ✏️ 딕셔너리로 분류별 합계
\`합계[분류] = 합계.get(분류, 0) + 금액\` 모양으로 누적합니다.` },
      { code: String.raw`
분류별_합계 = {}
for 지출 in 지출들:
    이름 = 분류하기(지출["항목"])
    # ✏️ TODO: 분류별_합계에 금액을 누적하세요
    pass

print(분류별_합계)
확인("도전 식비 합계", 분류별_합계.get("식비"), 88300)
확인("도전 전체 합계", sum(분류별_합계.values()), 252600)`,
        answer: String.raw`
분류별_합계 = {}
for 지출 in 지출들:
    이름 = 분류하기(지출["항목"])
    분류별_합계[이름] = 분류별_합계.get(이름, 0) + 지출["금액"]

print(분류별_합계)
확인("도전 식비 합계", 분류별_합계.get("식비"), 88300)
확인("도전 전체 합계", sum(분류별_합계.values()), 252600)` },
      { md: `## 도전 D. matplotlib 그래프
Colab에서 그래프에 한글을 쓰려면 한글 글꼴을 먼저 설치합니다. (처음 한 번, 약 20초)` },
      { code: String.raw`
import os, subprocess
import matplotlib.pyplot as plt
import matplotlib.font_manager as fm

글꼴 = "/usr/share/fonts/truetype/nanum/NanumGothic.ttf"
if not os.path.exists(글꼴):
    try:
        subprocess.run(["apt-get", "-qq", "install", "-y", "fonts-nanum"], check=False,
                       stdout=subprocess.DEVNULL, stderr=subprocess.DEVNULL)
    except FileNotFoundError:
        pass
if os.path.exists(글꼴):
    fm.fontManager.addfont(글꼴)
    plt.rcParams["font.family"] = "NanumGothic"
else:
    plt.rcParams["font.family"] = "Malgun Gothic"
plt.rcParams["axes.unicode_minus"] = False

if 분류별_합계:
    이름들 = sorted(분류별_합계, key=분류별_합계.get, reverse=True)
    plt.figure(figsize=(7, 3.5))
    plt.bar(이름들, [분류별_합계[k] for k in 이름들], color="#10684A")
    plt.title("3월 분류별 지출 (가상 데이터)")
    plt.ylabel("원")
    plt.tight_layout()
    plt.show()
else:
    print("도전 C를 먼저 완성하세요")` }
    ]
  }
};
