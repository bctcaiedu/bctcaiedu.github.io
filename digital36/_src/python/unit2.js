/* 2단원 파이썬 확장 — AI가 짠 코드 검증 · 코드 요청 템플릿 */
module.exports = {
  unit: 2,
  missions: [
    { part: 4, n: 'P3', h: 'AI가 짠 코드에서 오류 찾기 — "실행된다 ≠ 맞다"', body: `
      <p>글로 된 답은 틀려도 알아채기 어렵지만, 코드는 <strong>실행해 보면</strong> 틀린 곳이 드러납니다. "AI가 짜 준 코드"라고 가정한 3개(수업용으로 일부러 오류를 넣음)를 검증합니다.</p>
      <p><strong>쓰는 파이썬:</strong> 오류 메시지 읽기 · 함수 · 반복문 · 조건문 · 테스트 값 만들기</p>
      __NB__
      <div class="dl">
        <div class="dlrow"><span class="k">코드 1</span><span class="v">평균 구하기 — <strong>실행 오류</strong><small>없는 함수를 지어냄. 오류 메시지 마지막 줄: ____________</small></span></div>
        <div class="dlrow"><span class="k">코드 2</span><span class="v">윤년 판정 — <strong>조용히 틀림</strong><small>오류 없이 실행되지만 틀리는 연도: ______</small></span></div>
        <div class="dlrow"><span class="k">코드 3</span><span class="v">1부터 n까지 합 — <strong>경계값 오류</strong><small>합계(10)의 결과: ____ / 정답: ____</small></span></div>
      </div>
      <ol>
        <li>각 코드를 실행하고, 오류 메시지 또는 테스트 결과(❌)를 확인합니다.</li>
        <li>🤖 아래 질문으로 AI에게 고쳐 달라고 한 뒤, <strong>고친 코드를 다시 테스트</strong>합니다.</li>
        <li>✏️ 셀에 고친 코드를 넣어 테스트를 모두 ✅로 만듭니다.</li>
      </ol>
      <pre><span class="p">나 ▶</span> 아래 파이썬 코드를 실행했더니 이런 결과가 나왔어. 원인을 설명하고 고친 코드를 줘.
     [코드] (붙여 넣기)
     [결과] (오류 메시지 마지막 줄 또는 ❌ 테스트 줄 붙여 넣기)</pre>
      <div class="box check"><span class="lbl">오늘의 교훈</span>
        <p>AI의 답은 <strong>실행 오류</strong>처럼 티가 나기도 하지만, <strong>조용히 틀리는</strong> 경우가 더 위험합니다. 1900년 같은 <strong>예외 값</strong>과 끝 값으로 시험하는 습관이 교차 확인입니다.</p></div>` },

    { part: 6, n: 'P4', h: '프롬프트 노트에 "코드 요청 템플릿" 추가하기', body: `
      <p>좋은 질문의 4요소(역할·상황·요청·형식)는 코드를 부탁할 때도 그대로 통합니다. 템플릿으로 <strong>글자 수 세기 함수</strong>를 받아서, 미리 준비된 테스트로 검증합니다.</p>
      <p><strong>쓰는 파이썬:</strong> 함수 · 문자열 처리 · 반환값 여러 개 · 올림 계산</p>
      __NB__
      <pre><span class="p">나 ▶</span> [역할] 너는 파이썬 초보자를 가르치는 친절한 강사야.
     [상황] 나는 파이썬 기초(변수, 조건문, 반복문, 함수)까지 배웠고 Google Colab에서 실행할 거야.
     [요청] 문장을 받아 글자 수를 세는 함수 두 개를 만들어 줘.
       1) 글자수_세기(문장): (공백 포함 글자 수, 공백 제외 글자 수)를 돌려준다.
          공백 포함 = 줄바꿈만 뺀 모든 글자 / 공백 제외 = 띄어쓰기·줄바꿈·탭을 모두 뺀 글자
       2) 원고지_매수(문장): 공백 포함 글자 수를 200으로 나눠 올림한 값. 빈 문장은 0.
     [형식] 외부 라이브러리 없이, 줄마다 한국어 주석, 사용 예시 3개 포함.</pre>
      <ol>
        <li>위 질문을 AI에게 보내고, 받은 코드를 노트북 <strong>🤖 셀</strong>에 붙여 넣어 실행합니다.</li>
        <li>테스트 셀을 실행합니다. ❌가 있으면 <strong>❌ 줄을 그대로 AI에게 보내</strong> 고쳐 달라고 합니다.</li>
        <li>[요청]에서 "공백 제외" 정의를 빼고 다시 물어보면 결과가 어떻게 달라지는지도 확인합니다.</li>
        <li>완성된 템플릿을 프롬프트 노트 <strong>11번 템플릿</strong>으로 추가합니다.</li>
      </ol>
      <div class="box q"><span class="lbl">생각해 보기</span>
        <p>"글자 수 세 줘"만 말하면 AI는 공백을 셀까요, 뺄까요? <strong>요청이 모호하면 코드도 모호해집니다.</strong> 테스트를 먼저 정해 두면 요청이 분명해집니다.</p></div>` }
  ],

  notebook: {
    file: 'unit2_python.ipynb',
    title: '2단원 파이썬 확장 — AI가 짠 코드 검증 · 코드 요청 템플릿',
    cells: [
      { md: `# 2단원 파이썬 확장 실습
**디지털리터러시 36시간 과정 · ChatGPT 첫걸음: 질문하고 답받기**

| 파트 | 연결 차시 | 내용 |
|---|---|---|
| A | 4차시 AI가 틀릴 때 | AI가 짠 코드에서 오류 찾기 |
| B | 6차시 프롬프트 노트 | 코드 요청 템플릿으로 함수 받고 테스트로 검증 |

- **Shift + Enter**로 위에서부터 차례로 실행합니다.
- **✏️** 직접 채우기 · **🤖** AI에게 받은 코드 붙여 넣기
- 이 노트북의 오류는 **일부러 넣은 것**입니다. 빨간 오류가 나와도 당황하지 마세요.` },
      { code: String.raw`
# 테스트 도우미 — 먼저 실행하세요
def 테스트(함수, 사례):
    통과 = 0
    for 입력, 기대 in 사례:
        try:
            결과 = 함수(입력)
        except Exception as e:
            결과 = f"오류: {type(e).__name__}"
        ok = 결과 == 기대
        통과 += ok
        print(("✅" if ok else "❌"), f"{함수.__name__}({입력!r}) → {결과!r}   (기대: {기대!r})")
    print(f"--- {통과}/{len(사례)} 통과")
    return 통과 == len(사례)

print("준비 완료")` },

      { md: `---
## A. AI가 짠 코드에서 오류 찾기 (4차시)

아래 세 코드는 **"AI에게 부탁해서 받은 코드"** 라고 가정합니다. (수업용으로 AI가 흔히 하는 실수를 일부러 넣었습니다)

| 오류 종류 | 특징 | 찾는 법 |
|---|---|---|
| 실행 오류 | 빨간 오류 메시지가 바로 나옴 | 메시지 **마지막 줄** 읽기 |
| 논리 오류 | 오류 없이 실행되지만 **답이 틀림** | 답을 아는 값으로 테스트 |
| 경계값 오류 | 대부분 맞는데 **끝 값**에서 틀림 | 0, 1, 마지막 값으로 테스트 |

### 코드 1 — 요청: "점수 목록의 평균을 구하는 코드를 짜 줘"
👉 실행해 보세요. 빨간 오류가 나오는 것이 정상입니다.` },
      { code: String.raw`
# 🤖 AI가 준 코드 (그대로 실행)
import statistics

점수 = [80, 92, 75, 64, 88]
평균 = statistics.average(점수)
print("평균:", 평균)`, error: true },
      { md: `**오류 메시지 마지막 줄**을 읽어 봅시다: \`AttributeError: module 'statistics' has no attribute 'average'\`
→ "statistics 모듈에 average라는 것이 **없다**"는 뜻입니다. AI가 **그럴듯한 이름을 지어낸** 것입니다. (코드판 환각)

✏️ 고치는 방법은 두 가지입니다. 하나를 골라 아래 셀을 완성하세요.
1. 🤖 오류 메시지를 AI에게 보내 올바른 함수 이름을 받기 → **공식 문서에서 그 함수가 진짜 있는지 확인**
2. 배운 대로 \`sum()\`과 \`len()\`으로 직접 계산하기` },
      { code: String.raw`
점수 = [80, 92, 75, 64, 88]
평균 = None   # ✏️ TODO: 올바른 평균 계산으로 바꾸세요

print("평균:", 평균)
print("✅ 정답" if 평균 == 79.8 else "❌ 아직 (손계산: (80+92+75+64+88) ÷ 5 = ?)")`,
        answer: String.raw`
import statistics
점수 = [80, 92, 75, 64, 88]
평균 = statistics.mean(점수)      # 또는 sum(점수) / len(점수)

print("평균:", 평균)
print("✅ 정답" if 평균 == 79.8 else "❌ 아직 (손계산: (80+92+75+64+88) ÷ 5 = ?)")` },

      { md: `### 코드 2 — 요청: "연도를 넣으면 윤년인지 알려 주는 함수를 만들어 줘"
오류 없이 실행됩니다. 그런데 정말 맞을까요?

**윤년 규칙**: 4로 나누어떨어지면 윤년. 단, 100으로 나누어떨어지면 평년. 단, 400으로 나누어떨어지면 다시 윤년.` },
      { code: String.raw`
# 🤖 AI가 준 코드
def 윤년인가(연도):
    return 연도 % 4 == 0

print(윤년인가(2024), 윤년인가(2023))   # 여기까지는 맞아 보입니다
테스트(윤년인가, [(2024, True), (2023, False), (1900, False), (2000, True), (2100, False)])` },
      { code: String.raw`
# ✏️ TODO: 규칙대로 고치고 테스트를 모두 통과시키세요
def 윤년인가(연도):
    return 연도 % 4 == 0

테스트(윤년인가, [(2024, True), (2023, False), (1900, False), (2000, True), (2100, False)])`,
        answer: String.raw`
def 윤년인가(연도):
    if 연도 % 400 == 0:
        return True
    if 연도 % 100 == 0:
        return False
    return 연도 % 4 == 0

테스트(윤년인가, [(2024, True), (2023, False), (1900, False), (2000, True), (2100, False)])` },

      { md: `### 코드 3 — 요청: "1부터 n까지 더하는 함수를 만들어 줘"
👉 \`합계(10)\`은 55여야 합니다. 1+2+…+10을 떠올려 보세요.` },
      { code: String.raw`
# 🤖 AI가 준 코드
def 합계(n):
    total = 0
    for i in range(1, n):
        total += i
    return total

print("합계(10) =", 합계(10))
테스트(합계, [(1, 1), (3, 6), (10, 55), (100, 5050)])` },
      { code: String.raw`
# ✏️ TODO: 어디가 틀렸는지 찾아 고치세요 (힌트: range(1, n)은 n을 포함할까?)
def 합계(n):
    total = 0
    for i in range(1, n):
        total += i
    return total

# 다른 방법으로 교차 확인: 가우스 공식 n × (n + 1) ÷ 2
테스트(합계, [(1, 1), (3, 6), (10, 55), (100, 5050)])
print("공식으로 계산한 합계(100):", 100 * 101 // 2)`,
        answer: String.raw`
def 합계(n):
    total = 0
    for i in range(1, n + 1):
        total += i
    return total

테스트(합계, [(1, 1), (3, 6), (10, 55), (100, 5050)])
print("공식으로 계산한 합계(100):", 100 * 101 // 2)` },
      { md: `#### 💬 정리 (실습지에 적기)
- 코드 1은 **바로 티가 났고**, 코드 2·3은 **조용히 틀렸습니다.** 어느 쪽이 더 위험할까요?
- 코드 2를 2024·2023으로만 시험했다면 오류를 찾았을까요? → **예외 값**으로 시험하기
- 코드 3을 교차 확인한 방법은? → **다른 방법(공식)으로 같은 값을 계산**

👉 글로 받은 답도 똑같습니다. **답을 아는 질문으로 시험하고, 다른 자료로 교차 확인합니다.**` },

      { md: `---
## B. 코드 요청 템플릿으로 함수 받기 (6차시)

### B-1. 테스트를 먼저 정합니다
무엇이 "맞는 답"인지 먼저 정해 두면, **AI에게 할 요청도 분명해집니다.**

| 입력 | 글자수_세기 | 원고지_매수 |
|---|---|---|
| \`"안녕 하세요"\` | (6, 5) | 1 |
| \`""\` | (0, 0) | 0 |
| \`"a b\\nc"\` (줄바꿈 포함) | (4, 3) | 1 |
| \`"가" × 200\` | (200, 200) | 1 |
| \`"가" × 201\` | (201, 201) | 2 |

### B-2. 🤖 AI에게 요청하기
실습지의 **코드 요청 템플릿**을 ChatGPT나 Gemini에 보내고, 받은 코드를 **아래 셀에 통째로 붙여 넣어** 실행하세요.` },
      { code: String.raw`
# 🤖 여기에 AI가 준 코드를 붙여 넣으세요 (기존 두 줄은 지워도 됩니다)
def 글자수_세기(문장):
    return None

def 원고지_매수(문장):
    return None`,
        answer: String.raw`
import math

def 글자수_세기(문장):
    # 공백 포함: 줄바꿈(\n)만 뺀 모든 글자
    포함 = len(문장.replace("\n", ""))
    # 공백 제외: 띄어쓰기·줄바꿈·탭을 모두 뺀 글자
    제외 = len("".join(문장.split()))
    return (포함, 제외)

def 원고지_매수(문장):
    포함, _ = 글자수_세기(문장)
    return math.ceil(포함 / 200)

print(글자수_세기("안녕 하세요"), 원고지_매수("안녕 하세요"))` },
      { code: String.raw`
# 테스트 셀 — ❌가 나오면 그 줄을 복사해 AI에게 보내고 고쳐 달라고 하세요
ok1 = 테스트(글자수_세기, [("안녕 하세요", (6, 5)), ("", (0, 0)), ("a b\nc", (4, 3)),
                          ("가" * 200, (200, 200)), ("가" * 201, (201, 201))])
ok2 = 테스트(원고지_매수, [("안녕 하세요", 1), ("", 0), ("a b\nc", 1),
                          ("가" * 200, 1), ("가" * 201, 2)])
print("\n🎉 모두 통과! 템플릿을 프롬프트 노트에 저장하세요" if ok1 and ok2 else "\n조금만 더!")` },

      { md: `### B-3. 요청을 흐리게 하면?
템플릿의 [요청]에서 **"공백 포함/제외의 정의"를 빼고** 새 대화에서 다시 물어보세요. 받은 코드를 위 🤖 셀에 넣고 테스트하면 몇 개가 통과하나요?

### B-4. ✏️ 나만의 코드 요청 (도전)
템플릿으로 생활에 쓸 작은 함수를 요청해 보세요. **테스트를 먼저 3개 적고** 요청합니다.
- 예: \`디데이(목표날짜)\` → 오늘부터 남은 날 수 / \`평수_변환(제곱미터)\` → 평 (1평 = 3.3058㎡)` },
      { code: String.raw`
# ✏️ 1) 테스트를 먼저 적고  2) AI에게 요청해 받은 함수를 붙여 넣고  3) 실행
def 평수_변환(제곱미터):
    return None   # 🤖 AI 코드로 바꾸기

테스트(평수_변환, [(33.058, 10.0), (84, 25.4), (0, 0.0)])`,
        answer: String.raw`
def 평수_변환(제곱미터):
    return round(제곱미터 / 3.3058, 1)

테스트(평수_변환, [(33.058, 10.0), (84, 25.4), (0, 0.0)])` }
    ]
  }
};
